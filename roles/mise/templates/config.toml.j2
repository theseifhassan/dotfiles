# mise configuration — global defaults
# Intentionally minimal — mise works with sensible defaults out of the box.
# Per-project tool versions are managed via .mise.toml files in project directories.
