---
# roles/claude/defaults/main.yml

# Where the worktree-open hook script is deployed and what settings.json
# invokes — single definition so the two can't drift.
claude_worktree_hook: "{{ xdg_config_home }}/claude/hooks/open-worktree-in-zed"
