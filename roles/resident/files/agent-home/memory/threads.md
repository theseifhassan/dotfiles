# Open threads

> Things I'm actively watching. Remove a thread when it closes (note the close in journal.md).
> Format: `- [since YYYY-MM-DD] <thread> — waiting on: <what> — next check: <when>`

<!-- no open threads yet -->
