{
  "$comment": "The Resident — Claude Code session settings. The permission gate enforces the autonomy charter (decision D9: never --dangerously-skip-permissions).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$CLAUDE_PROJECT_DIR/hooks/permission-gate.py\""
          }
        ]
      }
    ]
  }
}
