{
  "icon_theme": {
    "mode": "light",
    "light": "Zed (Default)",
    "dark": "Zed (Default)"
  },
  "theme": {
    "mode": "dark",
    "light": "One Light",
    "dark": "Vercel Dark"
  },
  // Near-vim UI: no editor scrollbar.
  "scrollbar": {
    "show": "never"
  },
  // Worktree convention pin (this is Zed's default, declared for the record):
  // relative to each repo, so ~/Desktop/<repo> → ~/Desktop/worktrees/<repo>/…
  // — the same root SAW uses via SAW_WORKTREE_ROOT (mise role). Zed rejects
  // absolute paths here, which is why the shared root is expressed relatively.
  "git": {
    "worktree_directory": "../worktrees"
  },
  // Declutter the status bar — hide toggle buttons we don't use so the chrome
  // stays minimal. The panels themselves remain reachable via the command
  // palette / keymap; only their status-bar buttons are hidden.
  "agent": {
    "button": false
  },
  "collaboration_panel": {
    "button": false
  },
  "git_panel": {
    "button": false
  },
  "debugger": {
    "button": false
  },
  "global_lsp_settings": {
    "button": false
  },
  "search": {
    "button": false
  },
  "outline_panel": {
    "button": false
  },
  "agent_servers": {
    "claude-acp": {
      "type": "registry"
    }
  },
  "ssh_connections": [
    {
      "host": "macbook",
      "args": [],
      "projects": [
        {
          "paths": [
            "/Users/seifhassan/Projects/skydeo"
          ]
        },
        {
          "paths": [
            "/home/seifhassan/Desktop/skydeo/./"
          ]
        }
      ]
    },
    {
      "host": "macbook",
      "args": [],
      "projects": []
    },
    {
      "host": "forge",
      "args": [],
      "projects": [
        {
          "paths": [
            "/home/seifhassan/Desktop/learning/effect/./"
          ]
        }
      ]
    }
  ],
  "buffer_font_family": "Berkeley Mono",
  "buffer_font_size": 16,
  "vim_mode": true,
  "context_servers": {
    "skydeo-mcp": {
      "settings": {},
      "url": "https://skydeo-pve4npod2-skydeo.vercel.app/api/mcp",
    },
  },
}
