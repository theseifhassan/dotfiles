---
# roles/1password/tasks/main.yml
# Installs the 1Password desktop app — nothing else. Secrets consumed by
# these dotfiles come from Ansible Vault; no `op` CLI, no headless machinery.

- name: Install 1Password
  community.general.homebrew_cask:
    name: 1password
    state: present
